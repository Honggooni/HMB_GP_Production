# Security

보안 취약점이나 운영 경로를 공개 이슈에 게시하지 마십시오. 프로젝트
관리자에게 비공개 채널로 영향받는 버전, 재현 조건, 예상 영향과 최소
재현 자료를 전달하십시오. API 키, 정책 본문, 서명 키, 내부 미디어는
보고서에도 포함하지 않습니다.

## Repository visibility

이 저장소는 모든 HMB 팀원이 별도 저장소 권한 없이 업데이트를 받을 수
있는 **Public 팀 배포 저장소**입니다. 저장소와 GitHub Release는 Public을
유지합니다. 저장소 공개 여부를 보안 경계로 사용하지 않으며 API 키,
액세스 토큰, 비밀번호, 자격증명, 개인 서명키 또는 비공개 미디어를 절대
커밋하지 않습니다.

소스에 포함된 내부 호스트와 공유폴더 기본값은 팀 라우팅 정보일 뿐 접근
권한을 부여하지 않습니다. 실제 인증과 권한 검사는 해당 서버 및 공유
시스템에서 수행해야 합니다. 모든 변경은 `main` 반영 전에 전체 릴리스
보안 검사를 통과해야 합니다.

## 서명 정책 배포 경계

운영 ZIP에는 `resources/agent/hmb_agent_core.dat` 한 개만 포함됩니다.
이 파일은 서명된 v3 정책 봉투이며 압축·서명되어 있지만 암호화된 비밀
파일은 아닙니다. 공개 배포 가능한 검증 대상이며 API 키, 액세스 토큰,
개인 서명키 또는 저장소 자격증명을 포함하지 않습니다.

클라이언트에는 서명 검증용 공개키만 있으며 개인 서명키, API 키, 저장소
자격증명은 포함하지 않습니다. 릴리스 빌드와 CI는 정책 파일 SHA-256
`6152355dd51d68da33d4df197e6ac52f2c13b37d9644aa50efd9ba8c2cf13619`,
RSA 서명, 정책 버전 및 계약 해시를 모두 확인합니다. 이 파일 이외의
`.dat`, 개인키 형식, 자격증명 또는 토큰 파일은 소스와 ZIP에서 거부됩니다.

런타임은 설치된 라이브러리의 이 고정 로컬 파일만 읽으며 네트워크 공유,
환경변수 또는 임의 외부 경로에서 정책을 불러오지 않습니다. 동봉 파일을
교체하려면 승인된 오프라인 서명 절차와 고정 해시·회귀검사 갱신을 함께
수행해야 합니다. 운영 ZIP은 정확히 25개 파일만 포함하며 변경 이력
`CHANGELOG.md`와 내부 빌드 도구 `resources/build_release.py`는 배포하지
않습니다.
