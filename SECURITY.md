# Security

보안 취약점이나 운영 경로를 공개 이슈에 게시하지 마십시오. 프로젝트
관리자에게 비공개 채널로 영향받는 버전, 재현 조건, 예상 영향과 최소
재현 자료를 전달하십시오. API 키, 정책 본문, 서명 키, 내부 미디어는
보고서에도 포함하지 않습니다.

## Repository visibility

이 저장소는 모든 HMB 팀원이 별도 저장소 권한 없이 업데이트를 받을 수
있는 **Public 팀 Git 저장소**입니다. 정식 설치·업데이트 원본은 GitHub
`main`이며 팀원은 Git 메타데이터가 유지된 Griptape Library에서 Update
버튼을 사용합니다. GitHub Release, 다운로드 ZIP과 복사본은 지원 설치
경로가 아니며 등록된 Library를 대체하지 않습니다. CI가 생성하는 `dist`
ZIP은 게시하지 않는 로컬 검증 산출물입니다. 저장소 공개 여부를
보안 경계로 사용하지 않으며 API 키,
액세스 토큰, 비밀번호, 자격증명, 개인 서명키 또는 비공개 미디어를 절대
커밋하지 않습니다.

소스에 포함된 내부 호스트와 공유폴더 기본값은 팀 라우팅 정보일 뿐 접근
권한을 부여하지 않습니다. 실제 인증과 권한 검사는 해당 서버 및 공유
시스템에서 수행해야 합니다. 모든 변경은 `main` 반영 전에 전체 릴리스
보안 검사를 통과해야 합니다.

## 서명 정책 배포 경계

Git 저장소, 설치 패키지 및 CI 검증 ZIP에는 정확히 하나의 서명된 런타임
artifact인 `resources/agent/hmb_agent_core.dat`만 포함합니다. 영문 Canonical
정책, 한글 검토 정책 또는 정책을 복원할 수 있는 별도 문서는 포함하지
않으며 한글본은 로컬 논의·검토용으로만 유지합니다.

런타임은 설치된 Library의 이 고정 파일만 읽습니다. 환경변수, 서버 공유,
관리자 공유 또는 과거 경로로 대체하지 않습니다. 파일이 없거나 서명·내용을
검증하지 못하면 canonical Prompt→Agent 실행은 모델 호출 전에 중단됩니다.

`.dat`는 암호화 파일이 아니라 압축된 payload와 RSA 서명을 포함하는 무결성
artifact입니다. 클라이언트에는 검증용 공개키만 존재합니다. Master의 작은
정책 수정도 원문 확장자 변경이나 직접 편집으로 반영하지 않으며, 승인된
오프라인 서명 절차로 새 `.dat`를 만든 뒤 Library 업데이트와 함께
배포합니다.

signing private key, API 키, access token 및 저장소 자격증명은 GitHub와
릴리스 archive에 포함하지 않습니다. 중첩 ZIP까지 재귀 검사하여 추가
`.dat`, 정책 문서와 credential-like 파일을 거부합니다.
