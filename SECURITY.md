# Security

보안 취약점이나 운영 경로를 공개 이슈에 게시하지 마십시오. 프로젝트
관리자에게 비공개 채널로 영향받는 버전, 재현 조건, 예상 영향과 최소
재현 자료를 전달하십시오. API 키, 정책 본문, 서명 키, 내부 미디어는
보고서에도 포함하지 않습니다.

## Repository visibility

이 저장소와 GitHub Release는 팀 설치를 위한 공개 배포 채널로 사용할 수
있습니다. 배포물에는 공개 Broker CA와 기본 서비스 주소만 포함하며,
Bearer 토큰, 개인 키, 서명된 정책 본문, 팀 미디어는 포함하지 않습니다.
새 비밀 정보나 운영 자료를 추가하기 전에는 공개 릴리스 보안 검사를
반드시 다시 통과해야 합니다.

## Broker transport classes

Broker 전송 정책은 용도에 따라 분리합니다.

- **Agent 정책 Broker:** 예외 없이 HTTPS, 고정 공개 CA/peer 검증, DPAPI
  Bearer 인증과 서버 측 사용자 권한을 사용합니다. 아래 내부망 HTTP
  예외는 서명 정책 조회나 정책 본문 전달에는 적용되지 않습니다.
- **Seedance 생성 Broker 내부망 예외:** 기본 운영 주소
  `http://192.168.203.245:8080`은 클라이언트와 Broker가 모두 접근 통제된
  HMB 사내 제작망 안에 있고, 해당 포트가 인터넷·공용망·게스트망으로
  라우팅 또는 포워딩되지 않는 경우에만 사용할 수 있습니다. 이 예외는
  Seedance 계정 확인, capability 조회, 생성·상태·결과 회수 트래픽에만
  적용합니다. 인증은 기존 DPAPI Bearer와 서버 측 사용자 권한을 그대로
  요구하지만 HTTP 자체는 전송 암호화를 제공하지 않으므로, 이 조건에서는
  사내망 경계가 신뢰 경계입니다.

Seedance Broker가 위 사내망 경계를 벗어나거나 원격망, 공용 Wi-Fi, 인터넷,
외부 프록시 또는 포트 포워딩을 통과하면 내부망 HTTP 예외는 종료되며 HTTPS를
먼저 적용해야 합니다. 임의의 외부 HTTP 주소로 `HMB_AI_BROKER_URL`을 변경하는
것은 허용되지 않습니다.
